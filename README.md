# Binary Goldbach Circle-Method Framework:
# Explicit Off-Diagonal Positivity Certificate with Lean 4 Structural Verification

**Analytic estimates · rigorous Arb interval verification · Lean 4 structural verification**

**This repository does NOT prove Goldbach's Conjecture.**

[**API documentation**](https://negus333-conjecturepassion.github.io/Goldbach-sieve-density/docs) — browsable Lean declarations generated by doc-gen4.

This repository contains a developing formal and computational research framework for the Binary Goldbach Conjecture. The project began with a sieve-density and singular-series scaffold and has progressed into a circle-method program emphasizing displacement coordinates, structured off-diagonal cancellation, rigorous numerical certification, and Lean 4 structural verification.

## New result: Explicit Uniform Off-Diagonal Positivity Certificate

The repository now includes the accompanying v3.2 paper:

**An Explicit Uniform Off-Diagonal Positivity Certificate in a Displacement Coordinate for a Binary Goldbach Circle-Method Specialization**

*Explicit prime-distribution estimates, rigorous interval computation, and a Lean 4-verified structural bridge.*

For every even integer `N ≥ 56`, the three signed mod-3 off-diagonal residue imbalances studied in the paper are proved positive.

The result combines:

- explicit analytic estimates for the large ranges,
- rigorous Python-FLINT/Arb interval computation for the finite range, and
- Lean 4 verification of the structural displacement-support and core-domination bridge.

The public Lean extraction is available in:

[`Goldbach/OffDiagonalCertificate/`](Goldbach/OffDiagonalCertificate/)

The accompanying paper is available here:

[**Explicit Uniform Off-Diagonal Positivity Certificate v3.2 (PDF)**](paper/Explicit_Uniform_Off-Diagonal_Positivity_Certificate_v3_2.pdf)

[**LaTeX source for v3.2**](paper/Explicit_Uniform_Off-Diagonal_Positivity_Certificate_v3_2.tex)


## What is actually established

### Off-diagonal certificate

For the fixed specialization used in the paper,

`P = K = 3`, `Q = N / 3`, and `M = 2`,

the displacement coordinate is

`k = p + q - N`.

For every even `N ≥ 56`, the three signed mod-3 off-diagonal residue imbalances are positive.

Because the `M = 2` lobe-pair core contains the full prime-generated displacement support for the relevant even targets, this yields the corresponding favorable-core domination inequality.

This is an off-diagonal theorem: the zero-displacement fiber `k = 0` is not supplied by this result.

### Rigorous finite verification

The finite range

`56 ≤ N ≤ 454998`

was checked using rigorous Python-FLINT/Arb interval arithmetic.

The verification covered **227,472 even targets**, with:

- 225,475 targets resolved in Stage A,
- 1,997 targets resolved in Stage B,
- zero ambiguous cases, and
- zero failures.

These computations certify the finite portion of the off-diagonal theorem. They are not Lean computations.

### Lean 4 structural verification

The public Lean package verifies the structural part of the argument, including:

- prime-support finsets,
- ordered prime-support pairs,
- displacement `k = p + q - N`,
- the equivalence between zero displacement and the Goldbach representation fiber,
- positive and negative displacement-mass decomposition,
- mod-3 residue-cell imbalance,
- the full-support lobe-window argument for even targets, and
- the implication from positive signed imbalance to favorable-core domination.

The public extraction has successfully built with:

**3000/3000 jobs completed.**

The audited public package contains:

- zero `sorry`,
- zero `admit`,
- zero `sorryAx`,
- zero explicit project `axiom` declarations, and
- no imports from the private `Phase12PW_*` research tree.

The theorem audit reports only standard Lean/Mathlib foundational dependencies such as `propext`, `Classical.choice`, and `Quot.sound`.

## Scope: what is NOT proved

This repository does **not** prove the Binary Goldbach Conjecture.

The new theorem establishes a uniform positivity certificate for a specific off-diagonal displacement contribution.

The identity

`k = 0 ↔ p + q = N`

shows why this distinction matters: an actual Goldbach representation lies on the zero-displacement fiber, whereas the theorem established here concerns the off-diagonal contribution.

A separate quantitative lower bound for the complementary major-side contribution remains an independent mathematical gate before the present framework could imply Binary Goldbach.

The public Lean extraction should not be interpreted as a Lean formalization of the entire paper. The explicit prime-distribution estimates and Arb finite verification are established outside Lean; Lean verifies the structural bridge connecting the displacement geometry and core-domination implication.

## Earlier sieve-density foundation

The repository retains the earlier sieve-density formalization, including:

- base cases for small even integers,
- non-emptiness of a `{2,3}`-filtered candidate offset set,
- a uniform lower bound for the Hardy-Littlewood singular series, and
- subsequent exploratory circle-method, Vaughan-identity, Weyl-sum, and sieve developments.

These earlier files document the development path of the project and provide mathematical infrastructure for the broader research program.

## Building locally

```bash
lake exe cache get
lake build

## License

MIT (see `LICENSE`).

## Author

Dr. Gregory C. Stamp, Independent Researcher.
